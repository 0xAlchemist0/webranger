const promptHelper = {
  extractURL:
    "Given the following prompt extract the URl given within this prompt and return the URL only in text form prompt is after the :",
  validateMarkdown:
    "Given the following markdown does it have the right data to fufill the users prompt return only true or false:",
};

module.exports = promptHelper;
